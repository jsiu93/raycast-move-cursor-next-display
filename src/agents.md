# src/
> L2 | Parent: ../agents.md

Member List
move-cursor-command.ts: Shared Raycast command adapter, resolves `environment.assetsPath`, calls `assets/move-cursor`, maps helper stderr/stdout to Toast feedback.
move-cursor-next-display-center.ts: Raycast no-view command entrypoint, calls shared adapter with `next center`, maps result to Toast feedback.
move-cursor-next-display.ts: Raycast no-view command entrypoint, calls shared adapter with `next`, maps result to Toast feedback.
move-cursor-previous-display-center.ts: Raycast no-view command entrypoint, calls shared adapter with `previous center`, maps result to Toast feedback.
move-cursor-previous-display.ts: Raycast no-view command entrypoint, calls shared adapter with `previous`, maps result to Toast feedback.

Rule: Complete Members·One Line Per File·Parent Link·Technical Terms First

[PROTOCOL]: Update this header when changed, then check agents.md
